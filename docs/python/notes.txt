To build the python documentation:

- pip install the zonoopt module
- from this directory, "make html"
- open build/html/index.html

Image paths from the included README.md resolve automatically via the
docs/python/source/images symlink (-> ../../../images), so no manual
path fixup is needed.
